{
  "kind": "collectionType",
  "collectionName": "portfolios",
  "info": {
    "name": "Portfolio",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string"
    },
    "media": {
      "collection": "file",
      "via": "related",
      "allowedTypes": [
        "images",
        "files",
        "videos"
      ],
      "plugin": "upload",
      "required": false
    },
    "opis": {
      "type": "richtext"
    },
    "subtitle": {
      "type": "string"
    },
    "kategoria": {
      "type": "enumeration",
      "enum": [
        "www",
        "grafika"
      ],
      "default": "www",
      "private": true
    }
  }
}
